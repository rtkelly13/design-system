name: Storybook Walkthrough

# Captures every story in every theme and publishes Playwright's HTML report as
# a build artifact, with each story's three themes attached side by side.
#
# Deliberately a separate workflow from `ci.yml`, and deliberately not a gate:
# it asserts nothing about how things *should* look, it just makes what they do
# look like reviewable. Visual regression stays in `ci.yml` where it can block
# a merge.

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

# A walkthrough for an outdated commit is noise — drop in-flight runs when the
# branch moves on.
concurrency:
  group: walkthrough-${{ github.ref }}
  cancel-in-progress: true

jobs:
  walkthrough:
    name: Capture Screenshot Walkthrough
    runs-on: ubuntu-latest
    # The render environment, pinned: the official Playwright image at the
    # lockfile's Playwright version, by digest — rule 14's reasoning applied to
    # the one input no tracked file recorded. It carries Chromium and every
    # library it links, so nothing is installed at run time. Every job that
    # renders names this exact reference, and `check:governance` holds them
    # equal: a baseline written in one environment and checked in another is
    # the thing this suite can least afford. See docs/visual-regression.md.
    #
    # `--ipc=host` because Docker's default 64MB `/dev/shm` is what Chromium
    # renders into, and a full-page capture can exhaust it — Playwright's own
    # Docker guidance. `--init` reaps the browser's child processes.
    container:
      image: mcr.microsoft.com/playwright:v1.62.1-noble@sha256:dcc5531e97840b9b5e794f2814476b21571c5124a3fca2267d73041f56e7580e
      options: --ipc=host --init
    defaults:
      run:
        working-directory: packages/design-system
    # See ci.yml — same reason. This workflow is not a gate, so a run of it
    # sitting on a hung install for six hours is pure waste.
    timeout-minutes: 30

    steps:
      - name: Checkout Repository
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0

      # The container runs as root over a checkout the runner user owns, and
      # git refuses to read a repository owned by someone else ("dubious
      # ownership"). Every script that asks git for the repository root then
      # fails or falls back to the package — `check-lockfile.mjs` went looking
      # for `packages/design-system/pnpm-lock.yaml`, and `render-inputs.mjs`
      # produced no hash at all. `actions/checkout` trusts the path only for
      # its own git calls, so it is trusted here for everyone else's.
      - name: Trust the Workspace
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      # The report is a function of its inputs, the same ones the `visual` job's
      # verdict is keyed on (`scripts/render-inputs.mjs` — this workflow and the
      # walkthrough config are tracked files, so they count). When a run with
      # identical inputs already produced a report, a pull request restores it
      # and attaches it again rather than re-rendering every story on every
      # Level: ~3 minutes, and the report a reviewer downloads is byte-for-byte
      # the one those inputs produce. `main` always renders and saves, which is
      # what most pull requests restore from.
      - name: Rendering Inputs
        id: inputs
        # The render environment is the container above, so the key names its
        # digest; the host runner's image no longer reaches a pixel.
        # Assigned first, not substituted inside the `echo`: a failing `$(…)`
        # there leaves the step green and the key ending in `-`, one constant
        # every later pull request would match. As an assignment it fails the
        # step (bash -e), and the empty check covers a hash that printed nothing.
        run: |
          hash=$(node scripts/render-inputs.mjs)
          [ -n "$hash" ] || { echo "::error::render-inputs.mjs printed no hash"; exit 1; }
          echo "key=walkthrough-report-dcc5531e97840b9b5e794f2814476b21571c5124a3fca2267d73041f56e7580e-$hash" >> "$GITHUB_OUTPUT"

      - name: Previous Report
        id: report
        if: github.event_name == 'pull_request'
        uses: actions/cache/restore@0057852bfaa89a56745cba8c7296529d2fc39830 # v4.3.0
        with:
          path: packages/design-system/walkthrough-report
          key: ${{ steps.inputs.outputs.key }}

      - name: Setup
        if: steps.report.outputs.cache-hit != 'true'
        uses: ./.github/actions/setup

      - name: Build Storybook
        if: steps.report.outputs.cache-hit != 'true'
        run: pnpm build-storybook

      # The long pole of the whole PR, and the one that grows with the system:
      # every story adds four captures. `playwright.walkthrough.config.ts` runs
      # it at six workers for that reason — measured, see the note there.
      - name: Capture Walkthrough
        if: steps.report.outputs.cache-hit != 'true'
        run: pnpm walkthrough

      # Only a complete report is worth reusing, so only on success.
      - name: Save Report
        if: steps.report.outputs.cache-hit != 'true'
        uses: actions/cache/save@0057852bfaa89a56745cba8c7296529d2fc39830 # v4.3.0
        with:
          path: packages/design-system/walkthrough-report
          key: ${{ steps.inputs.outputs.key }}

      - name: Upload Walkthrough Report
        id: upload
        # Ships a partial report when a story fails to render — the failure is
        # exactly what you would want to look at. Deliberately not `always()`:
        # that also fires on cancellation, when `Capture Walkthrough` was
        # skipped and there is no report at all, so `if-no-files-found: error`
        # turned every cancelled run into a *failing* step. This workflow
        # cancels its own in-flight runs by design (see `concurrency` above),
        # so that was a self-inflicted red mark on routine behaviour.
        if: success() || failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: storybook-walkthrough-${{ github.event.pull_request.head.sha || github.sha }}
          path: packages/design-system/walkthrough-report/
          retention-days: 14
          if-no-files-found: error

      - name: Summary
        if: always()
        env:
          SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          {
            echo "## Storybook Walkthrough"
            echo
            if [ "${{ steps.report.outputs.cache-hit }}" = "true" ]; then
              echo "Restored, not re-rendered: these rendering inputs already produced this report"
              echo "(key \`${{ steps.inputs.outputs.key }}\`)."
              echo
            fi
            if [ -d walkthrough-report/data ]; then
              total=$(find walkthrough-report/data -type f -name '*.png' | wc -l)
              echo "Attached **${total}** screenshots (identical captures are deduplicated)."
            else
              echo "No screenshots were produced."
            fi
            echo
            echo "Download **storybook-walkthrough-${SHA}** below, unzip, and open"
            echo "\`index.html\` directly — it works from \`file://\`, no server needed."
            echo "Each test is one story; open it to see dark / dim / sketch together."
            if [ -n "${{ steps.upload.outputs.artifact-url }}" ]; then
              echo
              echo "[Artifact download](${{ steps.upload.outputs.artifact-url }})"
            fi
          } >> "$GITHUB_STEP_SUMMARY"
